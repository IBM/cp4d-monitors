{
  "name": "cloud-pak-deployer-monitors",
  "private": true,
  "version": "2.3.1",
  "license": "Apache 2.0",
  "scripts": {
    "dev": "gatsby develop -H 0.0.0.0",
    "dev:clean": "gatsby clean && gatsby develop -H 0.0.0.0",
    "build": "gatsby build",
    "build:clean": "yarn clean && gatsby build",
    "serve": "gatsby serve",
    "clean": "gatsby clean",
    "deploy": "gatsby build --prefix-paths && gh-pages -d public"
  },
  "dependencies": {
    "gatsby": "^3.3.1",
    "gatsby-theme-carbon": "^2.3.1",
    "react": "^17.0.2",
    "react-dom": "^17.0.2"
  }
}
